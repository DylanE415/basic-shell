A basic shell made in python that can run on mac/linux/windows shell than can execute any executables that are on the path.
If using on mac it can use vim command as you can see macvim is in the repo. 
type help to see all commands. 
Requires python>3.9 and readline>=8.1(to use up and down arrow to navigate command history)
